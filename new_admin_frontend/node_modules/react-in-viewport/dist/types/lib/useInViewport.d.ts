import { type RefObject } from 'react';
import type { Config, CallbackProps, Options } from './types';
declare const useInViewport: (target: RefObject<HTMLElement | null>, options?: Options, config?: Config, props?: CallbackProps) => {
    inViewport: boolean;
    enterCount: number;
    leaveCount: number;
};
export default useInViewport;
